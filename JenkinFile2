pipeline{
    agent any
    stages{
        stage("build"){
            step{
            echo "build for Asapz1"
            }
            
        }
        stage("deploy"){
            step{
            echo "Deploying artifact to staging server"
        }
        }
    }
}